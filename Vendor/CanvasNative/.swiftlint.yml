excluded:
  - Carthage
  - Vendor
  - Tests

disabled_rules:
  # I really like putting else one a new line in long guard statements.
  - statement_position

  # I prefer 2 new lines before a MARK.
  # TODO: Add a custom rule to enforce this.
  - trailing_whitespace

  # TODO: Remove this (lol)
  - todo

  # TODO: Remove this
  - function_body_length

  # TODO: Only allow `ID` to be shorter than 3
  - variable_name_min_length

  # TODO: Only allow uppercase variable names to start with `URL`
  - variable_name

# TODO: Change to 120
line_length: 256
